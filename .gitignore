# local backups created by installers
*.bak
*.bak.*
.DS_Store

# private project→type/color mappings (never commit — keeps internal names local)
cc-tab-overrides.sh
cc-notify.env
cc-error-patterns.txt
